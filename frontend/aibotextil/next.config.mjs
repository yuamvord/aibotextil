/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aquí podemos poner configuraciones extra si las necesitamos luego
  // Por ahora, lo dejamos limpio para asegurar que arranque.
};

export default nextConfig;
